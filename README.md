# website
Bakery Website 
## Changes

### Iteration 1 (Core Structure)
- **index.html**  
  - Added `<header>` with mission statement  
  - Built “Our Services” `<section>` with five `<article>` blocks  
  - Stubbed out `<nav>` and linked `/bakery.css`

- **bakery.css**  
  - Created placeholder CSS file

### Iteration 2 (Secondary Pages & Styling)
- **clients.html**  
  - Added “Intended Clients” table with `<thead>`/`<tbody>`  
- **team.html**  
  - Created “Our Team Roles” section with `<article>` and `<ul>` lists  
- **bakery.css**  
  - Defined typography scale, color variables, responsive flex layout for articles  
  - Styled `<table>` and `<nav>` hover states

### Iteration 3 (SEO & Performance)
- **index.html**, **services.html**, **clients.html**, **team.html**  
  - Added `<meta name="description">` to each page  
  - Tuned `<title>` tags with target keywords  
  - Ensured one `<h1>` per page, proper `<h2>`/`<h3>` hierarchy  
- **images/**  
  - Added `sourdough.jpg`, `croissant.jpg` with descriptive `alt` attributes  
- **sitemap.xml**  
  - Generated sitemap and linked it in `<head>` of each HTML  
- **robots.txt**  
  - Created to allow full crawling  
- **bakery.min.css**  
  - Generated minified CSS for faster load  
- **structured-data.jsonld**  
  - Added Bakery schema JSON‑LD in `index.html` `<head>`

